{% extends '::base.html.twig' %}
{% block title %}Manage Currency{% endblock %}
{% block page_title %}Manage Currency{% endblock %}

{% block body -%}

    <ul class="record_actions">
        <li>
            <a class="mdl-button mdl-js-button mdl-button--raised mdl-button--colored" href="{{ path('manage_currency_new') }}">Create a new Currency</a>
        </li>
    </ul>

    <table class="mdl-data-table mdl-js-data-table">
        <thead>
            <tr>
                <th class="mdl-data-table__cell--non-numeric">Code (ID)</th>
                <th class="mdl-data-table__cell--non-numeric">Htmlsymbol</th>
                <th class="mdl-data-table__cell--non-numeric">Name</th>
                <th class="mdl-data-table__cell--non-numeric">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td class="mdl-data-table__cell--non-numeric"><a href="{{ path('manage_currency_show', { 'id': entity.id }) }}">{{ entity.code }}</a> ({{ entity.id }})</td>
                <td class="mdl-data-table__cell--non-numeric">{{ entity.htmlSymbol }}</td>
                <td class="mdl-data-table__cell--non-numeric">{{ entity.name }}</td>
                <td class="mdl-data-table__cell--non-numeric">
                    <ul class="table-actions">
                        <li>
                            <a href="{{ path('manage_currency_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('manage_currency_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
