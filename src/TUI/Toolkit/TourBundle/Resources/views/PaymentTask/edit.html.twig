{% extends '::base.html.twig' %}
{{ form_javascript(edit_form) }}
{% block body -%}
    <h1>PaymentTask edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('paymenttask') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>

{% endblock %}
