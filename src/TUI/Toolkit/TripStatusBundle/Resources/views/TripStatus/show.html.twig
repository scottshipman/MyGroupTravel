{% extends '::base.html.twig' %}
{% block title %}Trip Status {{ entity.name }}{% endblock %}

{% block body -%}
    <h1>TripStatus</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Visible</th>
                <td>{{ entity.visible }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('manage_tripstatus') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('manage_tripstatus_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
