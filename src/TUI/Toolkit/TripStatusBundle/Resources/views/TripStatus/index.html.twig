{% extends '::base.html.twig' %}
{% block title %}Manage Trip Status{% endblock %}

{% block body -%}
    <h1>TripStatus list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Visible</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('manage_tripstatus_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.visible }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('manage_tripstatus_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('manage_tripstatus_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('manage_tripstatus_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
