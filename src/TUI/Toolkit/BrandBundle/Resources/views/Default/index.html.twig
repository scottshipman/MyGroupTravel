{% if brand.media %}
    <div class="logo">
        <img src="{{ asset(".." ~ brand.media.getRelativePath ~ "/" ~ brand.media.getHashedFilename ) }}">
    </div>
{% else %}
    {{ brand.name }}
{% endif %}
