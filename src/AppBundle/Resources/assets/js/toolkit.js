(function ($) {
  /*
  $('input:checkbox').each( function() {
    var id = $(this).attr('id');
    $(this).addClass('form-checkbox').hide();
    $('label[for=' + id + ']').addClass('checkbox-replace');
    if ( $(this).is(':checked') ) {
      $('label[for=' + id + ']').addClass('checked');
    };
  });

  $('.form-checkbox').change(function() {
    var id = $(this).attr('id');
    $('label[for=' + id + ']').toggleClass('checked');
  });
  */
})(jQuery);
